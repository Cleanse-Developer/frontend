"use client";
/**
 * Navbar — the site header. Thin re-export of the existing components/Menu/Menu,
 * so all of its logic (scroll states, locale capsule, overlay menu, GSAP
 * scramble) and visuals are preserved exactly. Migration notes in
 * docs/COMPONENT_MAP.md.
 */
export { default } from "@/components/Menu/Menu";
